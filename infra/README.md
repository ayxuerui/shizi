# infra

Cloudflare Worker (sync endpoint) and D1 (hosted event store) configuration.

Not yet implemented — see `openspec/changes/bootstrap-shizi-assessment/tasks.md`
Section 9 ("Infra & deploy"). This directory is scaffolded now so the
workspace structure matches `design.md`'s architecture ahead of that work.
