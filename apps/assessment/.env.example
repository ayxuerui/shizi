# Both undefined until Section 9 (Cloudflare Worker + D1 sync endpoint)
# ships — see src/offline/endpoint.ts. Until then, flushQueue() always
# returns {status: "skipped"}; the app is fully usable offline regardless.
VITE_SYNC_ENDPOINT=
VITE_SYNC_TOKEN=
